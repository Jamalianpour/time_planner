name: time_planner_example
description: An example for time planner

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  time_planner:
    path: ../
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
